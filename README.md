# webcrawler-php
A simple web crawler  implemented in PHP
## Run the application
### Build the container
`docker build -t oworyoakim/webcrawler-laravel .`
### Run the container
`docker-compose up -d`
